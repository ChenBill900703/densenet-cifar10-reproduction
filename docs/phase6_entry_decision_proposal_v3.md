# Phase 6 Entry Decision Package v3

Status: **APPROVED AS D-046 / CANONICAL ASSEMBLY AND FRESH PREFLIGHT REQUIRED / EXECUTION NOT YET STARTED**

Date prepared: **2026-08-25**

Approved verbatim by the human on **2026-08-25**. Phase 6 is re-entered at
new candidate formal optimizer calls zero, but execution remains fail closed
until D-046 is committed, the non-circular D-045/D-046 capability is assembled
and verified, and a fresh exact-account preflight passes.

Evidence class: `DERIVED` for the frozen identities, verified technical state,
old-run preservation, storage observation, and measured deadline projection;
`IMPLEMENTATION-ASSUMPTION` for administrative execution/stop governance.
This package contains no prediction, accuracy, evaluation, aggregation, or
paper-match result.

## Sole eligible execution baseline

| Item | Identity |
|---|---|
| Annotated tag | `formal-freeze-densenet-bc100-12-cifar10plus-ledger-performance-corrected-2026-08-24` |
| Tag object | `f9b2250c9769ebd2e5e58e877c77aa1c11d45cc2` |
| Tag target / D-045 approval commit | `728c63ba8c7204e665ae0a9070ee74b67cb068d3` |
| Freeze source | `863375d4082abaa2a7f6580e4f90c3ec114cbce3` |
| Freeze record | `0ac24e07f54342428b698297db689f1408ea0f43` |
| Schema-v2 manifest | SHA256 `6CC22F7D918DF1689C4E14A33E8BB4FDAF502EF51149AF1E6537D2618547EC26` |
| Config | SHA256 `C5F1B2DA0B2E0A5476531C0460313849EEB4058400BED0E0EFC3227D82A83213` |
| Dataset | SHA256 `C4A38C50A1BC5F3A1C5537F2155AB9D68F9F25EB1ED8D9DDDA3DB29A59BCA1DD` |
| Corrected project wheel | SHA256 `D31FE8A0DFBDBF4B4100C28E587DDDA98A13EE63219B373143DF41C01F8CE859` |
| Python runtime | SHA256 `BAC14B77461752024EBF95A820B1791F2061FD2B5B368E4D3BAD9A307460716F` |
| Installed environment | SHA256 `1E0D0EA18AE43BCBEDA2962EB363C7D8CE7FBB8B2B03000D33A8BEA130A7C953` |
| Corrective machine report | SHA256 `5BA5972212E1A55BC1BEBC28CB9AEAA69CC7235F46ECB9B1D9F51B717C137286` |
| D-043 stop report | SHA256 `8D3E7E83D654AF42CFB8DEE673BD8E0232DE48063CE586B74F13C31E2BF98F23` |

All earlier freeze tags and D-025/D-035 entry authorities are historical and
execution-ineligible. D-028 remains exactly two zero-byte SHA256-empty files.
The old `15CB...` namespace remains permanently incomplete, immutable,
abandoned, and non-resumable with 24,421 physical calls. No old checkpoint,
ledger call, accepted coordinate, or trajectory may be loaded, migrated, or
combined into the new run.

## Non-circular entry capability

If this package is approved, the approval must first be committed as D-046.
A later record-only commit must then create and bitwise verify:

1. a canonical D-045 corrective-freeze-completion decision JSON bound to the
   D-045 approval commit and accepted tag/manifest tuple;
2. a distinct canonical D-046 Phase 6 entry decision JSON bound to its later
   approval commit and this exact authorization;
3. a SHA256-bound authorization JSON naming both decision artifacts and the
   new manifest.

Any schema, decision kind, approval commit, path, SHA256, tag object/target,
manifest, wheel, runtime, environment, dataset, or D-043 stop-report mismatch
must fail closed before any mutable run path, model, optimizer, dataset, or
loader is created.

## Mandatory fresh preflight

After canonical authorization assembly, one fresh exact live preflight must
run under `<REDACTED_EXECUTION_ACCOUNT>`, SID
`<REDACTED_EXECUTION_SID>`, using the frozen offline
runtime and corrected wheel. Before mutation it must verify:

- D-045/D-046 canonical capability and every accepted freeze identity;
- Windows/Python, driver, RTX 3070 Ti UUID/capability, deterministic IEEE
  FP32, AMP/TF32/compile/recomputation/accumulation disabled as frozen;
- prepared manifest plus only `data_batch_1.bin` through
  `data_batch_5.bin`; training preflight must not stat/open/hash/map/decode
  `test_batch.bin`;
- storage, D-028, all earlier tags, the complete abandoned old run, absence of
  later-seed/test/evaluation artifacts, and absence of the new
  `6CC22F...` namespace.

Current read-only readiness observes 40,997,347,328 free bytes versus
7,164,705,960 required, no project Python process, and no new namespace. These
observations do not replace the mandatory post-approval preflight.

## Authorized execution sequence if approved

Only after every prior gate passes may the runner create the new full-manifest
namespace and execute the unchanged seeds strictly in this order:

1. seed `1021082110`, epoch 1 through epoch 300;
2. only after seed 1 epoch-300 artifacts are immutable and hash-verified,
   seed `1747066946`, epoch 1 through epoch 300;
3. only after seed 2 epoch-300 artifacts are immutable and hash-verified,
   seed `869460408`, epoch 1 through epoch 300.

Each seed uses FP32 physical batch 64, workers 2, 300 retained checkpoints,
and the append-only intent/completion ledger. A normal interruption may use
only the frozen epoch-boundary/initial-boundary rollback rules; physical calls
are never truncated. Any unresolved intent or unknown/unexpected exit stops
automatic progress and requires a separately evidenced disposition. No old
checkpoint may be used.

Only after all three epoch-300 training sets are immutable and hash-verified
may `test_batch.bin` be accessed. Each seed may then receive exactly one final
test evaluation in fixed order, followed by the frozen aggregation schema.

## Deadline and fail-closed rules

D-045 accepted the `OBSERVED-WEEK-FEASIBLE` derived estimate of
91.0979797-94.35848545 training hours (about 3.80-3.93 days). This is not a
guarantee. Interruption, host contention, evaluation, approval latency, or a
fail-closed event may extend it.

Any account/SID, authorization, artifact, path, hash, environment, GPU,
prepared access, storage, ledger, checkpoint, order, finite-value, or process
state failure; OOM; unresolved intent; interrupted evaluation; or protocol
discrepancy must stop automatic progress. It may not be worked around by
changing batch, precision, AMP, TF32, workers, accumulation, recomputation,
compile, seed, data, model, loss, SGD, LR, checkpoint, test access,
evaluation, aggregation, or reporting. Pretrained results, post-hoc tuning,
best-seed/best-epoch selection, repeated test attempts, and test-guided
changes remain forbidden. After the first new formal optimizer call, this
baseline is immutable. No paper-match claim is allowed before final results.

## Exact human authorization required

**「我批准 Phase 6 entry decision package v3：確認 annotated tag `formal-freeze-densenet-bc100-12-cifar10plus-ledger-performance-corrected-2026-08-24`、tag object `f9b2250c9769ebd2e5e58e877c77aa1c11d45cc2`、tag target／D-045 approval commit `728c63ba8c7204e665ae0a9070ee74b67cb068d3`、corrective freeze-source commit `863375d4082abaa2a7f6580e4f90c3ec114cbce3`、corrective freeze-record commit `0ac24e07f54342428b698297db689f1408ea0f43`、schema-v2 corrective freeze manifest SHA256 `6CC22F7D918DF1689C4E14A33E8BB4FDAF502EF51149AF1E6537D2618547EC26`、canonical config SHA256 `C5F1B2DA0B2E0A5476531C0460313849EEB4058400BED0E0EFC3227D82A83213`、dataset SHA256 `C4A38C50A1BC5F3A1C5537F2155AB9D68F9F25EB1ED8D9DDDA3DB29A59BCA1DD`、corrected project wheel SHA256 `D31FE8A0DFBDBF4B4100C28E587DDDA98A13EE63219B373143DF41C01F8CE859`、Python runtime archive SHA256 `BAC14B77461752024EBF95A820B1791F2061FD2B5B368E4D3BAD9A307460716F`、installed environment manifest SHA256 `1E0D0EA18AE43BCBEDA2962EB363C7D8CE7FBB8B2B03000D33A8BEA130A7C953`、corrective machine report SHA256 `5BA5972212E1A55BC1BEBC28CB9AEAA69CC7235F46ECB9B1D9F51B717C137286` 與 D-043 controlled-stop report SHA256 `8D3E7E83D654AF42CFB8DEE673BD8E0232DE48063CE586B74F13C31E2BF98F23` 為唯一可申請新執行的正式基線；三個較舊 freeze tags、D-025／D-035 與舊 manifest namespace 僅保留為歷史且禁止用於新執行，D-028、abandoned old-run 62 個 artifacts 及其 24,421 次 physical calls 必須永久保持 immutable／incomplete／abandoned／non-resumable，禁止 resume／migrate／combine／truncate／delete／replace，且新 source 禁止載入任何舊 checkpoint。批准在 new candidate formal optimizer calls=0 的狀態重新進入 Phase 6，但必須先以 D-046 approval commit 非循環建立並逐位元驗證 D-045 corrective-freeze-completion、D-046 phase6-entry 兩份 canonical decision JSON 及其 SHA256-bound authorization JSON，任何 schema／decision kind／approval commit／path／hash／tag object／tag target／manifest／wheel／runtime／environment／dataset／D-043 stop report 不符均禁止執行；批准僅在正式帳戶 `<REDACTED_EXECUTION_ACCOUNT>`／SID `<REDACTED_EXECUTION_SID>` 的 fresh exact live preflight 通過後，才可建立新的 full-manifest-hash namespace，且 preflight 必須在任何 mutable run path／model／optimizer／dataset／loader 建構前驗證全部 authorization／artifact／environment／GPU／storage／D-028／舊 tags／abandoned old-run／new namespace absence，之後只驗證 prepared manifest 與 `data_batch_1.bin` 至 `data_batch_5.bin`，在三個 epoch-300 training artifacts 全部 immutable 且 hash-verified 前禁止 stat／open／hash／map／decode `test_batch.bin`。批准只使用 frozen offline runtime、corrected project wheel、approved Toronto CIFAR-10 binary archive、FP32 physical batch 64、workers=2 與 project seeds `1021082110`、`1747066946`、`869460408`，嚴格依序先完整執行 seed `1021082110` 的 epoch 1 至 300，僅在其 epoch-300 artifacts immutable/hash-verified 後執行 seed `1747066946` 的 epoch 1 至 300，再僅於其完成驗證後執行 seed `869460408` 的 epoch 1 至 300；每 seed 保留 300 checkpoints 與 append-only optimizer intent/completion ledger。批准正常中斷僅依 frozen epoch-boundary／initial-boundary rollback 規則續跑，rollback physical calls 不得截斷；任何 unresolved intent、unknown／unexpected exit 或不一致狀態必須停止自動進度並另行處置。僅在三個 seeds 的 epoch-300 training artifacts 全部 immutable 且 hash-verified 後，才可依固定順序各執行一次 final-test evaluation，之後才可依 frozen schema aggregation。任何 account／SID／authorization／artifact／path／hash／environment／GPU／prepared access／storage／ledger／checkpoint／order／finite-check／process failure、OOM、unresolved intent、interrupted evaluation 或 protocol discrepancy 均必須 fail closed，不得改變 batch、precision、AMP、TF32、workers、accumulation、recomputation、compile、seed、data、model、loss、SGD、LR、checkpoint、test-access、evaluation、aggregation 或 reporting 規則，不得使用 pretrained results、post-hoc tuning、best-seed／best-epoch selection、repeated test attempts 或 test-guided change；接受 91.0979797 至 94.35848545 小時的 `OBSERVED-WEEK-FEASIBLE` 僅為 DERIVED estimate 而非七日保證；第一個新正式 optimizer call 後 baseline 不可變更，正式結果完成前不得宣稱對上論文結果。」**
