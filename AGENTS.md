# DenseNet Reproduction Governance

## Project state

- Current lifecycle state: **D-060 PROJECT CLOSED / FINAL DELIVERY TAGGED - NO FURTHER WORK AUTHORIZED IN THIS LIFECYCLE**.
- The human approved the formal target on **2026-08-16** with the exact authorization: **「我批准此 DenseNet formal target」**.
- Approved target: **DenseNet-BC-100-12 / CIFAR-10+ / FP32 / batch 64 / 300 epochs**.
- Target approval resolves target-selection blocker `B-001`. It is **not** the Phase 5 configuration freeze and does not resolve any other blocker.
- On **2026-08-16**, the human approved `Phase 2 entry decision package v1` with the exact authorization recorded in `docs/decision_log.md`. This resolves policy decisions `B-002` through `B-006` and approves `A-001`, `A-002`, `A-003`, `A-004`, `A-005`, `A-006`, and `A-008` within their recorded scope. It is not the Phase 5 freeze and does not authorize optimizer construction or training.
- Phase 1 passed a superseding 47/47-test comprehensive revalidation plus independent code, numerical, environment, and governance audits on **2026-08-16**. The original 27-test milestone remains preserved by its historical tag. Neither milestone is a formal reproduction result.
- Phase 2 may validate exact CIFAR artifacts, decoding, preprocessing, augmentation, samplers, DataLoader behavior, and RNG replay without training. Completed Phase 1 code may receive correctness/audit maintenance, but its scope must not silently expand.
- Phase 2 technical obligations passed on **2026-08-16**. On **2026-08-23**, the human approved `Phase 2 completion decision package v1` verbatim, resolving A-007/H-001/M-001, H-003, M-002, and M-003 within their recorded scope and completing Phase 2. This approval explicitly continues to prohibit Phase 3, optimizer/scheduler construction, and formal training; a separate Phase 3 entry decision is required.
- On **2026-08-23**, the user restored the exact locked arXiv-v5 PDF at `docs/1608.06993v5.pdf`. Default-path source verification passed for 19 files and five repositories, and the strict combined suite passed 61/61. This evidence-maintenance result does not grant Phase 3 or optimizer authority.
- On **2026-08-23**, the human approved `Phase 3 entry decision package v1` verbatim. Phase 3 may implement and test loss, SGD, explicit epoch LR, runtime RNG mapping, and checkpoint mechanics. Optimizer steps are authorized only on completely generated data and must be classified as non-formal synthetic mechanics steps. CIFAR optimizer steps, CIFAR training, prediction, accuracy, pretrained results, Phase 4, Phase 5 freeze, and formal optimizer steps remain forbidden; formal optimizer steps remain zero.
- Phase 3 technical obligations passed on **2026-08-23**: the strict combined suite passed 115/115, the deterministic GPU checkpoint replay was bit-exact, and the machine report recorded five generated-only optimizer calls and zero formal optimizer steps. On the same date, the human approved `Phase 3 completion decision package v1` verbatim, completing Phase 3 within the recorded scope while explicitly withholding Phase 4 and every CIFAR/formal optimizer authority.
- On **2026-08-23**, the human approved `Phase 4 entry decision package v1` verbatim. Phase 4 may execute exactly 16 non-formal generated-data optimizer calls (Worker A 11; Worker B replay 5), exact-device memory/timing measurement, and—only after an `OBSERVED-FIT` result—one bounded train-mode raw-logit forward over exactly 64 approved CIFAR training samples.
- Phase 4 technical obligations passed on **2026-08-23** at source `f91cdf6ee5e8fafd20148af3313b3a56a16e6747`: exact-device disposition `OBSERVED-FIT`, all fresh-process replay checks bit-exact, the bounded 64-sample raw-logit forward passed, and the final project/fresh suites passed 135/135.
- On **2026-08-23**, the human approved `Phase 4 completion decision package v1` verbatim, accepting the exact technical record and completing Phase 4 within its measured scope. Phase 5 entry/freeze and every CIFAR/formal optimizer authority remain explicitly withheld.
- On **2026-08-23**, the human approved `Phase 5 entry and freeze-assembly decision package v1` verbatim. Phase 5 may implement and statically/mock validate the canonical config, schemas, append-only ledger, fail-closed runner, artifact lock, offline reconstruction, source bundle, and storage gate. It may download and hash dependency artifacts, archive the Python runtime, reverify the approved dataset hash without model execution, and rerun the existing generated-only regression suite.
- Phase 5 technical obligations passed on **2026-08-23** at freeze-source `5d5d6d89cde00134776a59924896758f30816281` and freeze-record `29254a5153b500b77a61027fe356364c75cacade`. The manifest SHA256 is `2EF356BF70F9C89C73E03D86D0726F0DA736D73A2FC6B7CC9255DFC1557E3DD1`; project and offline formal-wheel suites passed 156/156.
- On **2026-08-23**, the human approved `Phase 5 completion and formal freeze decision package v1` verbatim, accepted the complete D-020 identity tuple, completed Phase 5, and authorized annotated tag `formal-freeze-densenet-bc100-12-cifar10plus-2026-08-23`. A-015 through A-020, H-009 through H-012, M-006 through M-008, and L-001 are frozen within their recorded scope.
- Phase 6 has **not** been entered. Any CIFAR model forward/loss/backward/optimizer/training/test evaluation/prediction/accuracy/result aggregation, pretrained result, or formal optimizer step remains forbidden. Formal optimizer steps remain zero. A separate human-approved Phase 6 entry package is mandatory before formal execution.
- The **2026-08-23 Phase 6 entry readiness audit** found three pre-execution runner blockers: H-013 (no frozen-CLI recovery path after an epoch-1 interruption), H-014 (formal seed training order is not enforced), and H-015 (decision hashes are not verified against the named decision artifact files). No formal execution occurred. The first freeze tag remains preserved, and D-024 now supersedes it for any future execution.
- On **2026-08-23**, the human approved `formal-freeze corrective assembly decision package v1` verbatim. Only static/mock/generated-only correction and validation of H-013 through H-015 plus affected artifact rebuilding were authorized. The original freeze tag must not move or be deleted. D-024 subsequently completed the corrective freeze; Phase 6 entry and every CIFAR/model/loss/backward/optimizer/evaluation/result operation remain forbidden, and formal optimizer steps remain zero.
- Corrective technical obligations passed on **2026-08-24** at source `9efdd584f664df3b9f74ac9917e3b389400d61ec`: H-013 through H-015 guards passed, project/fresh offline formal-wheel suites passed 166/166, and the corrected manifest SHA256 is `64CFB2826BFE6D77CB9EE15E0BEF544186D51947C843A96C7C9F2DD9D82CABC7`.
- On **2026-08-24**, the human approved `formal-freeze corrective completion decision package v1` verbatim, accepted the complete D-024 corrective identity tuple, closed H-013 through H-015 for the corrected baseline, and authorized annotated tag `formal-freeze-densenet-bc100-12-cifar10plus-corrected-2026-08-24` as the only freeze eligible for future execution. The original tag `formal-freeze-densenet-bc100-12-cifar10plus-2026-08-23` must remain unchanged at `4e69d397f7935ea2f4f9eedc83ecf43547946626` as historical evidence.
- Phase 6 has **not** been entered. The corrective completion approval authorizes no CIFAR/model forward, loss, backward, optimizer, training, test evaluation, prediction, accuracy, aggregation, pretrained result, or formal optimizer step. Formal optimizer steps remain zero. A separate human-approved Phase 6 entry package is mandatory before formal execution.
- On **2026-08-24**, the human sent the short message **「批准 Phase 6 entry decision package」** before any such package or canonical Phase 6 decision artifact existed. This is recorded as a request to prepare `docs/phase6_entry_decision_proposal.md`, not as D-025 or execution authority, because treating it as approval would bypass H-015 and leave the exact scope/hash/stop rules unspecified. The exact authorization in that proposal must be approved verbatim before Phase 6 entry.
- Later on **2026-08-24**, the human approved the exact `Phase 6 entry decision package v1` authorization verbatim. This is D-025 and enters Phase 6 at formal optimizer steps zero. Before any model/data/formal-root mutation, the approval must be committed, the non-circular D-024/D-025 canonical decision JSON files and SHA256-bound authorization JSON must be assembled and committed, and H-015 plus exact live launch/storage checks must pass. Only then may the frozen runner begin seed `1021082110`; every fail-closed and ordering rule in the approved package remains binding.
- D-026 assembled the non-circular canonical decision capability after D-025: D-024 decision SHA256 `A3D63FCF420089EDB6718289B74070415E92A763C1F8F37170B305E046694AED`, D-025 decision SHA256 `01900484F754B33E7A1B4C85352CA0274F6094399B99AEF2B3A21234759A41AD`, and authorization SHA256 `F42C70ACF5E1F718D8419EAB2EF2587734B6485DD2C67F4EC60D609FBFC50B35`. The corrected offline wheel passed H-015 without model/data construction. Exact live launch/storage/formal-root preflight remains mandatory before seed 1.
- D-027 exact live preflight passed under the corrected offline wheel with `CUBLAS_WORKSPACE_CONFIG=:4096:8`: all freeze/config/dataset/wheel/runtime/environment/GPU identities matched, current storage exceeded the frozen gate, `runs/formal` was empty, and no model/data was constructed. Its report SHA256 is `C13301EF3B3BFCCC6D6CE078F3C0E3B2BC238A3EE16FA215DAFF13B66A11520C`. Seed `1021082110` may now start only through the frozen runner and D-025 capability; formal optimizer steps remain zero until its first ledgered call.
- The first seed-1 command then failed closed before dataset construction completed because execution account `<REDACTED_EXECUTION_ACCOUNT>` received `WinError 5` on the existing prepared CIFAR directory. The frozen control flow had constructed model/optimizer, but decoded samples, optimizer intents/completions, accepted steps, checkpoints, and checkpoint manifests all remain zero. The seed directory contains only two 0-byte SHA256-empty ledger/progress files; do not delete, truncate, replace, recreate, resume, or migrate them. H-016 blocks automatic progress.
- The subsequent read-only corrective audit found H-017 (prepared training access/integrity is absent from preflight before formal-root/model/optimizer mutation) and H-018 (the train-split verifier SHA256-reads `test_batch.bin` before the all-training-complete gate). The human selected the full corrective-freeze direction, but source/ACL correction requires the exact authorization in `docs/phase6_preflight_acl_corrective_assembly_decision_proposal.md`. The earlier sandbox-account resume proposal is superseded and must not be approved or used. Until approval, source, ACL, data bytes, and the D-028 run remain unchanged; formal optimizer calls remain zero.
- On **2026-08-24**, the human approved `Phase 6 preflight/ACL corrective assembly decision package v1` verbatim as D-030. This authorizes only the bounded H-016/H-017/H-018 source/tests/evidence correction, minimal additive read/traverse ACL grant with before/after byte and security-descriptor evidence, and affected artifact rebuilding. D-028 must remain immutable and abandoned. No formal run, evaluation, aggregation, formal tag, or optimizer call is authorized; technical completion still requires separate corrective-freeze completion approval and then separate Phase 6 re-entry approval bound to the new manifest.
- D-031 records corrective source candidate `d36d1db36b05405a882dcd6ea4b4205d8ed3d364`: ACL-independent tests passed 173/173, the complete suite passed 174/175, source verification passed 19/19 files and 5/5 repositories, and deterministic wheel SHA256 is `EA442A88E04665096FA1BA872516DA5604203183B0B6BE5E2A2D3587C9876E19`. The sole failure is the approved prepared-data replay receiving the same `WinError 5`. The sandbox owner-context helper is unavailable, so no ACL mutation was attempted. Corrective completion, manifest, tag, and Phase 6 re-entry remain forbidden; formal optimizer calls remain zero.
- On **2026-08-24**, the human approved `Codex sandbox helper recovery v1` verbatim as D-032. Before/after evidence proves that only a non-inheriting `WRITE_DAC/ChangePermissions` ACE for `<REDACTED_EXECUTION_ACCOUNT>` was added to `.git`; owner, inheritance protection, every non-target ACE, Git objects/index/tracked bytes, HEAD, and clean state remained unchanged. This restored the `<REDACTED_SANDBOX_ACCOUNT>` owner-context helper.
- The first authorized prepared-directory grant attempt correctly stopped after Windows `icacls /T` unexpectedly changed root inheritance protection. The failure snapshot is preserved. Root protection was restored to its exact pre-grant Boolean state, all non-target ACE semantics and all seven prepared-file size/SHA256 identities match the before evidence, and the only effective access delta is one allowed RX/synchronize ACE for the approved account on the root inherited by the seven approved files. The repository ACL script now uses `Set-Acl` on only the protected root and compares ACEs as semantic sets; `icacls.exe` is forbidden by its static guard.
- The post-ACL project suite passed 175/175, source verification passed 19/19 files and 5/5 repositories, and the Phase 2 data-pipeline diagnostic passed under the exact `<REDACTED_EXECUTION_ACCOUNT>` account with workers 0/2 bit-exact and model/optimizer/accuracy operations all zero. D-028 remains exactly two zero-byte SHA256-empty files.
- D-033 records the completed technical candidate: freeze-source `47028a6b4ab38b007e59ce763cc01d21824abad0`, freeze-record `b3a18133743b26d5e0f0054eebccd0adafdf3dae`, schema-v2 manifest SHA256 `15CB6FD32E5D15D33F1EAF1F716938BC80A73C1E1466E3AA44108E5E08FFDAC1`, project wheel SHA256 `DECE8F41A0ACDDAC6869F38A3C9FE147196C799150544870CE59FB426BB7904D`, installed environment manifest SHA256 `00B4D3295826F22617FCF438F6C1E344E95686729BE7F82B6F5B0C4D440CF0B0`, and machine report SHA256 `96304D146FD3BC1FBD9A5B039140A9D373868367C9F55345E74D169F30E0CAE7`. Project/fresh formal-wheel suites passed 175/175, 20/20 wheels and 8,264/8,264 runtime files reverified, and exact formal-account launch/storage preflight passed without model/data/optimizer construction.
- On **2026-08-24**, the human approved `Phase 6 preflight/ACL corrective freeze completion decision package v1` verbatim as D-034. This accepts H-016 through H-019 and the complete source/record/schema-v2-manifest/wheel/runtime/environment/report identity tuple, authorizes annotated tag `formal-freeze-densenet-bc100-12-cifar10plus-preflight-acl-corrected-2026-08-24`, and preserves both older tags and D-028 unchanged. The approval does not revive D-025 and authorizes no Phase 6 execution, CIFAR/model operation, training, evaluation, aggregation, or optimizer call. A separately prepared and human-approved Phase 6 entry package bound to the new tag and manifest remains mandatory; formal optimizer calls remain zero.
- The D-034 annotated tag exists with tag object `5695e67b37a5d5eec3fc8bedf04af0ffabf312e8` and peels to approval commit `86e478eb49c0d3674a3a288e19d6dfe5a95803eb`. `docs/phase6_entry_decision_proposal_v2.md` is the only pending Phase 6 entry request. It must be approved verbatim before D-035 canonical authorization assembly or any live execution. Formal optimizer calls remain zero.
- Later on **2026-08-24**, the human approved `Phase 6 entry decision package v2` verbatim as D-035. Phase 6 is re-entered at formal optimizer calls zero, but execution remains fail-closed until the D-035 approval commit is recorded, distinct canonical D-034/D-035 decision JSON and their SHA256-bound authorization JSON are assembled in a later record-only commit, the frozen runner verifies all identities, and a fresh exact-account live preflight passes before mutation. D-028 and both older tags remain immutable historical evidence.
- D-036 assembles and frozen-wheel verifies the non-circular D-034/D-035 capability: D-034 decision SHA256 `1EFECAD7307DF6084991FD5A06AB0831C881B8C7B0E428AFB64AE92F317EBD16`, D-035 decision SHA256 `3CEFC8F3E79FE1931AAB74F19F4CC663D1A87A58E143BF96DB89AF93AD24B969`, authorization SHA256 `FB32BBFA7A5ED87D170C189C85F85C5FC9348D0D9242A15C71D502F666BB0397`, and assembly report SHA256 `83CCE2E95D580AB1A1F1C055DD8E7222A5430632AC7D39887FCA2267E0F16A0E`. No model/data/optimizer/formal-root mutation occurred. A fresh exact live preflight remains mandatory before any formal run namespace may be created; formal optimizer calls remain zero.
- D-037 fresh exact live preflight passed under `<REDACTED_EXECUTION_ACCOUNT>` with the frozen offline wheel. D-034/D-035 capability, all freeze/config/dataset/wheel/runtime/environment/GPU identities, deterministic IEEE FP32 policy, five training batches, storage, D-028, and the absence of the new namespace were verified before mutation; `test_batch.bin` was not touched. The first wrapper attempt completed preflight but failed only while formatting its summary; the corrected second wrapper produced the canonical report SHA256 `68B7D76558BD5A6B545F8E7CFF2BCD31D2EECC33650BFCD2F58682F344DFCC2E`. No model/dataset object/loader/optimizer/run namespace was constructed. Seed `1021082110` is now the only legal next formal run; formal optimizer calls remain zero until its first ledgered call.
- D-038 records the first immutable formal boundary for seed `1021082110`: epoch 1 completed with exactly 782 accepted/physical calls at the boundary; checkpoint SHA256 `0E9791D2BC7BCF2E128456CFBDCF12B2F7FC5F08359B3C9D20A3EB799D6B5675` and manifest SHA256 `0330CE19FD05946D91AD9DB1385803E67F4F9487FFE4A9D7D055B803639F8768` match. Training continued into epoch 2. The baseline is now immutable. Current physical-call count is dynamic and must be read from the append-only ledger; it is at least 782. No test access, evaluation, prediction, accuracy, or aggregation is authorized while training remains incomplete.
- The seed-1 process later stopped during epoch 14 after batch index 761. Read-only frozen verification found 10,928 intents/completions, zero unresolved intents, 13 valid checkpoints, a valid complete progress log, no temporary artifact, and no later seed/test/evaluation state. The latest rollback checkpoint was epoch 13; cause remains `UNKNOWN` because no terminal status or relevant Windows OOM/GPU/power/crash event exists.
- Later on **2026-08-24**, the human approved the exact `Phase 6 seed-1 unknown-interruption/epoch-13 rollback resume disposition package v1` as D-039. The cause remains `UNKNOWN`. D-039 permits exactly one rollback resume from the verified epoch-13 checkpoint after a fresh frozen pre-resume reverification; it preserves all 10,928 prior physical calls and requires a full 782-call replay of epoch 14. It forbids initial-boundary recovery, a new namespace/seed directory, epoch-1 restart, artifact truncation, test access, later seeds, and a second automatic resume.
- D-040 fresh pre-resume reverification passed under the exact formal account and frozen offline wheel. D-034/D-035 authorization, every launch identity, five training batches only, storage, D-028, seed order, all 13 checkpoints, the complete progress log, and the exact `[10928,10928]` ledger were reverified; epoch-13 remained the sole rollback checkpoint, unresolved intents were zero, and no model/dataset object/loader/optimizer was constructed. Report SHA256: `107E991DEBB2062B495F3D7555701CDF4E1B95AD17199D3F2B8C432F12F302C3`.
- D-041 records the successful D-039 rollback boundary. The frozen offline runner resumed the original seed directory only from exact `epoch-013.pt`; all 10,928 prior physical calls remain append-only evidence. Epoch 14 was replayed for exactly 782 calls and published `epoch-014.pt` at accepted trajectory 10,948 and physical interval `[11710,11710]`. Checkpoint SHA256 is `23B2BE5F71064AD794F5479FF6D84E632A1F69F424626F05C427BF33F53467BF`; manifest SHA256 is `FB066EB2C92A0C75F8FC073E7ACA1ACCB2502DAFBF94474E871A4C16F88AF0B8`; ledger head is `4BF3C096089EDB1374DFB9954A76A1E3A81004A8C5ECD7755A08AED8DDA2A28A`. Frozen ledger/progress/payload verification passed, test records accessed remain zero, and training continued into epoch 15. The original interruption cause remains `UNKNOWN`; D-039 still authorizes no second automatic resume.
- A one-week professor deadline exposed H-020: the frozen append path re-runs the complete ledger verifier after every intent and completion, and observed epoch intervals increased from 806.417 seconds at epoch 15 to 1,187.785 seconds at epoch 29. Readiness report SHA256 `F8B28DF76FC63A6281C243838EE28D8DC4BA3353DFBA30190480BEBC37D6CA33` classifies the current runner as deadline-infeasible without claiming a scientific error. The exact corrective proposal records the bounded stop, preservation, correction, and later-gate requirements.
- Later on **2026-08-24**, the human approved `Phase 6 ledger-performance corrective assembly decision package v1` verbatim as D-042. D-042 authorizes one evidence-first controlled stop of only the verified old formal process tree, permanent immutable abandonment of that incomplete namespace, and static/mock/generated-only H-020 incremental-ledger correction and affected artifact rebuilding. It authorizes no new CIFAR/formal execution, test access, evaluation, aggregation, or paper-match claim. Corrective completion and new Phase 6 entry remain separate approvals.
- D-043 records the completed D-042 stop. Cooperative `CTRL_BREAK` was unavailable with `WinError 87`; after exact identity reverification, only the two workers, frozen trainer, console helper, and wrapper were terminated child-first. Frozen post-stop verification passed with 24,421 intents, 24,421 completions, unresolved intents 0, exact physical interval `[24421,24421]`, 30 checkpoints/manifests, complete finite progress, no temporary/final-test/later-seed artifact, and no remaining project Python/GPU process. Latest checkpoint is `epoch-030.pt`, SHA256 `F0FEC4B9B65D725C053B5A6285A2554BF0B98EA66D34C77B41694E40E00F03B6`; run-tree SHA256 is `5BA8FB33B8307D72EEE390D7FCCF2141C9CE9A0990A23ACDF8371755F0403C37`. The namespace is permanently incomplete, abandoned, non-resumable, and immutable. Stop report SHA256: `8D3E7E83D654AF42CFB8DEE673BD8E0232DE48063CE586B74F13C31E2BF98F23`.
- D-044 records the completed H-020 technical candidate: freeze-source `863375d4082abaa2a7f6580e4f90c3ec114cbce3`, freeze-record `0ac24e07f54342428b698297db689f1408ea0f43`, schema-v2 manifest SHA256 `6CC22F7D918DF1689C4E14A33E8BB4FDAF502EF51149AF1E6537D2618547EC26`, wheel SHA256 `D31FE8A0DFBDBF4B4100C28E587DDDA98A13EE63219B373143DF41C01F8CE859`, installed manifest SHA256 `1E0D0EA18AE43BCBEDA2962EB363C7D8CE7FBB8B2B03000D33A8BEA130A7C953`, and machine report SHA256 `5BA5972212E1A55BC1BEBC28CB9AEAA69CC7235F46ECB9B1D9F51B717C137286`. Project/fresh strict suites passed 191/191 each; source, wheelhouse, runtime, exact-account/GPU/prepared-training/storage, D-028, older tags, and all 62 old-run artifacts reverified. The derived disposition is `OBSERVED-WEEK-FEASIBLE` at 91.0979797-94.35848545 hours for 900 epochs, explicitly not a guarantee. Separate corrective-freeze completion approval remains mandatory before the proposed tag, followed by a separate new Phase 6 entry; new candidate formal optimizer calls remain zero.
- On **2026-08-25**, the human approved `Phase 6 ledger-performance corrective freeze completion decision package v1` verbatim as D-045. D-045 accepts the complete D-044 identity tuple and `OBSERVED-WEEK-FEASIBLE` disposition, completes the corrective freeze, and authorizes annotated tag `formal-freeze-densenet-bc100-12-cifar10plus-ledger-performance-corrected-2026-08-24`. It authorizes no new Phase 6 execution, CIFAR operation, test access, evaluation, aggregation, or optimizer call. D-028, the abandoned old namespace and its 24,421 calls, and all earlier tags remain immutable; a separate manifest/tag/stop-report-bound Phase 6 entry package is mandatory, and every new seed must start from epoch 1 in a new namespace.
- The D-045 annotated tag exists with tag object `f9b2250c9769ebd2e5e58e877c77aa1c11d45cc2` and peels to approval commit `728c63ba8c7204e665ae0a9070ee74b67cb068d3`. `docs/phase6_entry_decision_proposal_v3.md` is the only pending entry request. It must be approved verbatim before D-046 approval recording, non-circular canonical authorization assembly, fresh exact-account preflight, namespace creation, or any new formal optimizer call.
- Later on **2026-08-25**, the human approved `Phase 6 entry decision package v3` verbatim as D-046. Phase 6 is re-entered at new candidate formal optimizer calls zero. Execution remains forbidden until the D-046 approval commit exists, distinct canonical D-045/D-046 decision JSON plus SHA256-bound authorization are assembled and frozen-wheel verified in a later record-only commit, and a fresh exact-account preflight passes before any new namespace/model/optimizer/dataset/loader mutation. Only seed `1021082110` may start, from epoch 1; old checkpoints are forbidden.
- D-047 assembles and frozen-wheel verifies the non-circular D-045/D-046 capability: D-045 decision SHA256 `26765193321F36917385439CA1E5D1E2A75DD7E81732F851896839CEA0F691E2`, D-046 decision SHA256 `2DEF2D32999B516F95B88F39B289108715E58A83C61763A25FE358CFF704E8E5`, authorization SHA256 `A32119C4ED208543DA51BD19173A09FC7FD5672A0CCAB99CB69C39469E2C6508`, and assembly report SHA256 `18216FA3AAB6A96B32B77592FD3AE04B85B46C00A772AAF2824D08AAE2309C00`. No model/dataset/optimizer/new namespace mutation or test-byte access occurred. A fresh exact-account live preflight remains mandatory before Seed 1; new candidate formal optimizer calls remain zero.
- D-048 fresh exact live preflight passed under `<REDACTED_EXECUTION_ACCOUNT>` with the corrected frozen offline wheel. D-045/D-046 capability, all freeze/config/dataset/wheel/runtime/environment/GPU identities, deterministic IEEE FP32 policy, five training batches, storage, all four tags, D-028, and every one of the abandoned old run's 30 checkpoint manifests plus complete ledger/progress were verified before mutation; `test_batch.bin` was not touched and the new `6CC22F...` namespace was absent. Canonical report SHA256 is `583B54FA0B32580ED2FEF29B6061D2E9744E1E0FB0CE4BD6CF4A787A1383A16A`. Seed `1021082110` may now start only from epoch 1 through the frozen runner; new formal optimizer calls remain zero until its first ledgered call.
- D-049 records the completed and frozen-wheel-verified Seed `1021082110` training artifact. All 300 checkpoints and 300 manifests passed path/schema/size/SHA256/provenance verification; the canonical finite progress log has exactly 234,600 records; the complete ledger has exactly 234,600 intents and 234,600 completions, unresolved intents 0, physical interval `[234600,234600]`, and head `AAC18C52A3F22E255566F4E7E5BD1ECF9BF67FBA49B8A81AC1C90676342C09E7`. Epoch-300 checkpoint SHA256 is `5E5197F5D75E3D5CDE9C2CED5FCCBB0C2948965B67E7FDAFF99BB9DECBDF4811`; the verification report SHA256 is `166B62BCA6C4960567E90B3FBB67FE4D7766A711642E097F6A0F10C5BD573F39`. No later-seed directory or final-test evidence exists and test records accessed remain zero. Seed `1747066946` is the only legal next training seed; test access remains forbidden.
- D-050 records the Seed `1747066946` launch boundary after a fresh exact-account/process/storage check and a second full frozen verification of D-049. The frozen runner passed its own preflight before creating the Seed-2 directory. At the canonical snapshot, epoch 1 batch index 191 had exactly 192 accepted/physical completions, ledger sequence 384 ended in completion, two workers were alive, and stdout/stderr were empty. Report SHA256 is `741F00CC9F4B7C3D4E0C8A3C53EECE74394720BB8CF3C2DFBF7760595C7AB7B8`. Seed 2 baseline is now immutable; current calls are dynamic and must be read from its append-only ledger. Seed 3 and all test access remain forbidden.
- D-051 records the completed and frozen-wheel-verified Seed `1747066946` training artifact. All 300 checkpoints/manifests, exactly 234,600 canonical finite progress records, and the complete 234,600-intent/234,600-completion ledger passed; unresolved intents are 0, physical interval is `[234600,234600]`, and ledger head is `A8CD8BB46720BB83149FE953D93CE55F2611FED0E5A138E18C2627B2916597C9`. Epoch-300 checkpoint SHA256 is `FF8180B8FC1B147E6CA91BDEEB5BB449D37D5E133505CBC8B3AC779353531387`; report SHA256 is `68D76433B70DB08B046E9BDDE774E0AAF6BA734AA4159AF0DEE16CD1047B1CE5`. Seed 3 and final-test evidence remain absent, and test records accessed remain zero. Seed `869460408` is the only legal next training seed.
- D-052 records the safe launch boundary for Seed `869460408`. Exact account/SID, process absence, fresh log targets, storage, D-049/D-051 report hashes, and the frozen Seed-3 order gate passed; the gate reverified both earlier seeds at exactly 234,600 completed calls and unresolved intents 0 before mutation. The runner then passed its internal preflight before creating Seed 3. At the consistent snapshot, epoch 1 batch index 66 had exactly 67 accepted/physical completions and ledger sequence 134 ended in completion; two workers were alive and stderr was empty. Report SHA256 is `75019B2D1972B73A53EEBD60D4061C016979DD537448EB627D6FE13453BADEC9`. Seed 3 baseline is immutable; test access remains forbidden until its epoch-300 artifact is complete and all three seeds reverify.
- D-053 records the completed and frozen-wheel-verified Seed `869460408` artifact and closes the all-training-complete gate. Seed 3 has 300 checkpoints/manifests, exactly 234,600 canonical finite progress records, 234,600 intents/completions, unresolved 0, physical interval `[234600,234600]`, and ledger head `B1914753A1247DDE72E1E76E9C8BAFA6FA652129272048AD84FA6B1A51743963`; epoch-300 checkpoint SHA256 is `EC466CFD49C9C732EA94F92AD3D8A7C2DC252DD2BD300852A5DF62A68371C0A8`. Report SHA256 is `394F9C859045F6E21488A7C6270CF3284CE17BE56B680FCBAA6EC9367160623B`. All three training seeds are now immutable/hash-verified and test records accessed remain zero. The only legal next action is one final-test evaluation per seed in fixed order, followed by frozen aggregation.
- D-054 records Seed `1021082110`'s one permitted final-test evaluation after D-053. The canonical attempt, all 157 sequential progress records covering exactly 10,000 examples, and result passed frozen schema/hash verification. Incorrect count is 466, exactly 4.66% error; test attempts equal 1. Result SHA256 is `6D67DD39B18079347DAAD38413113A20D1B553132D33D96107C3561788F5DF92`; verification report SHA256 is `555A56B8AD02C2E8C7DC9B3C3698C590C3BE5DB9C5D4C2F6B605BEBB7F54FB10`. This is one partial formal result, not an aggregate or paper-match claim. Seed `1747066946` is the only legal next evaluation.
- D-055 records Seed `1747066946`'s one permitted final-test evaluation. Its canonical attempt, 157 sequential progress records, exactly 10,000 examples, and result passed. Incorrect count is 461, exactly 4.61% error; result SHA256 is `7A42A217A2A30DF30B6C6BEE75DCFFF9629B77AD8588C4D915E2A72D8BCBF84C`; report SHA256 is `AAD30CE615B1F80CB95E2F8E428191C7E4D88671FC7DBE7C70CC95E3D27D286F`. These are two partial formal results; aggregation and paper-match claims remain forbidden. Seed `869460408` is the only legal next evaluation.
- D-056 records Seed `869460408`'s one permitted final-test evaluation. Its canonical attempt, 157 sequential progress records, exactly 10,000 examples, and result passed. Incorrect count is 481, exactly 4.81% error; result SHA256 is `D274D0B8BF5AE6E10803666EC022A66D3416F8EAE89F3138EF206506B3FC97B4`; report SHA256 is `63866185C002F059C932A0960725806F858D67F894D4ABD7303BC54EBBC6B511`. All three fixed-order single final tests are complete with exactly one attempt each. Frozen aggregation is the only legal next action; no paper-match claim may precede aggregate verification.
- D-057 records the completed frozen aggregation over ordered incorrect counts `[466,461,481]`. The immutable aggregate reports individual errors `[4.66%,4.61%,4.81%]`, exact mean `352/75%` (`4.693333333333%`), and sample standard deviation `0.104083299973` percentage points with selection `none`. Aggregate SHA256 is `A2669C814149C11101B9963B7FC6F24248EE80BAA674D8721628A80166F6D46A`; verification report SHA256 is `85921C4A779F8D1169AEF817FA8DC248E607D085B4578A0E9219CED65D773A9B`. Phase 6 formal execution and aggregation are complete; comparison to the paper must use the locked primary evidence and remain explicit about the metric/statistical mismatch.
- D-058 records the primary-evidence comparison and final reproduction analysis. The locked arXiv-v5 paper Table 2 value is 4.51%; the frozen reproduction mean is 4.693333333333%, an exact difference of `11/60` percentage points (`+0.183333333333` pp). The conclusion is `NUMERICALLY-CLOSE-NOT-IDENTICAL-NO-STATISTICAL-EQUIVALENCE-CLAIM`; no post-hoc tolerance is created because paper seed, run count, variance, aggregation, and exact cuDNN build remain unknown. Analysis report SHA256 is `54A40BDB9D4D4F07A8316C8EC1C331EEBA6D4DD4B3FA34E62ED2001DE74BF690`.
- D-059 records the completed professor-defense delivery. `DenseNet_CIFAR_Reproduction_Final.pptx` contains 14 rendered and full-size-inspected slides, 14/14 `[Sources]` speaker-note blocks, no overflow, no unintended overlap, and no unresolved placeholder; SHA256 is `D5E0D94D376B10CE74327AD452831A1E50315A2FAC6642C57D016CA133FAF6EA`. The final report, evidence index, 26-question defense Q&A, and presentation script are committed with their exact hashes. Delivery report SHA256 is `88CE130EE1992944583F697A039BCB713A45CC92B69FDDC8EA93DE125DA2AC8E`. The requested DenseNet reproduction lifecycle is complete; later exploratory experiments would require a separate target and must not rewrite these formal artifacts.
- D-060 records the human-directed project closure on **2026-08-29** with the exact authorization **「我想結束該專案了」**. The formal scientific freeze, three completed formal runs, three final-test results, aggregate, comparison, reports, Q&A, script, formal deck, and plain-language derivative deck are closed and immutable. The untracked `tmp/` directory was removed only after its sole file was proven byte-identical to the retained formal deck; it contained no unique evidence. No training, evaluation, aggregation, experiment, or rewrite is authorized within this lifecycle. Any later work must begin as a separately governed project and must not move the scientific freeze or alter these artifacts.

## Evidence-first rules

Use this evidence order:

1. User-supplied original paper PDF.
2. Author-controlled official repository and its pinned history.
3. Historical framework/dependency source.
4. Results directly derived from items 1-3.
5. Explicitly human-approved implementation assumptions.

Every material claim must be classified as one of:

- `PAPER-SPECIFIED`
- `OFFICIAL-CODE-SPECIFIED`
- `HISTORICAL-DEPENDENCY-BACKED`
- `DERIVED`
- `IMPLEMENTATION-ASSUMPTION`
- `UNKNOWN`
- `FORMAL-REPRODUCTION-RESULT`

Never invent a value. If the evidence does not determine a value, write `UNKNOWN`. The pinned repository is the source of truth for implementation behavior; the paper remains higher priority for the claimed experimental protocol. Any disagreement must stay visible in `docs/evidence_conflicts.md` and must not be silently resolved.

## Prohibited conduct

- No values filled from memory, blogs, common practice, torchvision defaults, or third-party reimplementations.
- No post-hoc tuning, seed selection, aggregation changes, checkpoint-selection changes, or test-set-guided changes.
- No best-test result substituted for a frozen final-result rule.
- No protocol changes after formal results exist.
- No formal optimizer step before target approval, Phase 1-4 validation, and Phase 5 freeze.
- No change to batch size, precision, AMP, TF32, image resolution, or gradient accumulation to make a run fit without prior human approval and a new freeze.

## Phase gates

- A `BLOCKER` stops entry to the next phase.
- Any setting that changes the training trajectory is at least `HIGH`.
- High-impact paper/code conflicts require human approval.
- After the first formal optimizer step, the frozen baseline cannot change. A genuine code bug invalidates all affected formal runs and requires a new freeze and full rerun.

## Formal-result governance

- Seeds must be preregistered as `PROJECT REPRODUCTION SEEDS`, never described as paper seeds unless evidence is found.
- Every seed uses the same source commit, frozen config hash, dataset hash, environment lock, selection rule, aggregation rule, and checkpoint rule.
- Formal aggregation must consume hash-verified run artifacts and follow the frozen rule exactly.
- Report the paper result and the observed frozen reproduction result honestly, including disagreement.
